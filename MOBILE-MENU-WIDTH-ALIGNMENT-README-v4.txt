MOBILE MENU WIDTH + ALIGNMENT FIX V4

This version adds a stronger override:
- the dropdown panel is anchored to the right edge of the header container
- the dropdown width now shrink-wraps to the real content width
- the panel no longer keeps the old stretched width from previous CSS
- cache-bust updated to menufix4

If Safari still shows old layout, it is almost certainly cached CSS.
