MOBILE MENU WIDTH + ALIGNMENT FIX

What changed:
- Mobile dropdown is now anchored to the RIGHT side of the header.
- The menu panel is no longer stretched across unnecessary empty width.
- Width is now content-driven with a controlled minimum and maximum width.
- Padding inside the panel was reduced to remove excess white space.
- Long items like "Academic Background" still fit, but the panel ends much closer to the visible text.

Files changed:
- assets/css/multipage.css
- all HTML pages now use cache-bust version menufix3
