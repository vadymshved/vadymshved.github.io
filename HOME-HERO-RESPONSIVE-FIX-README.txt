HOME HERO RESPONSIVE FIX

What was improved:
- The hero section now keeps a two-column layout for longer when the browser narrows.
- The right visual block scales down more gradually instead of causing collisions with the left-side tags.
- The mini-cards inside the right block become more compact on tablet widths.
- At smaller widths the right block stacks below only after the layout becomes too narrow.
- The portrait image now uses controlled crop positioning.

Main breakpoints:
- 1320px: slightly tighter hero columns
- 1120px: compact right visual block
- 980px: right block becomes narrower and mini-cards stack vertically
- 820px: very compact side visual
- 700px and below: single-column mobile stacking

Portrait crop:
- object-position: center 32%
You can change this value later if you want the face slightly higher or lower.
